#!/bin/bash

LIBRARY="$1"
UNIVERSE="$2"
LANGUAGE="$3"
MODULE="$4"
READING="$5"
PERMISSION="$6"

if [ ! -d "$LIBRARY-$UNIVERSE-$LANGUAGE-$MODULE" ]; then mkdir "$LIBRARY-$UNIVERSE-$LANGUAGE-$MODULE"; fi

make_division "$LIBRARY" "$UNIVERSE" "$LANGUAGE" "$MODULE" "alias" "$READING" "$PERMISSION"
make_division "$LIBRARY" "$UNIVERSE" "$LANGUAGE" "$MODULE" "inductor" "$READING" "$PERMISSION"
make_division "$LIBRARY" "$UNIVERSE" "$LANGUAGE" "$MODULE" "identity" "$READING" "$PERMISSION"
make_division "$LIBRARY" "$UNIVERSE" "$LANGUAGE" "$MODULE" "embedding" "$READING" "$PERMISSION"
make_division "$LIBRARY" "$UNIVERSE" "$LANGUAGE" "$MODULE" "structure" "$READING" "$PERMISSION"

